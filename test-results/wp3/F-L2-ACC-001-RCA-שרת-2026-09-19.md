# F-L2-ACC-001 · RCA (שרת) · 2026-09-19

**Mode:** ROOT CAUSE ONLY · **NO fix · NO patch · NO dpl · NO Acc rewrite**  
**Build:** `dpl_7vAAEXkrSBYfS2XVR2VyujmxV3rR` (frozen)

---

## 1. Minimal reproduction

**Input (fixed):**
```http
POST https://akvot-simple-demo.vercel.app/api/lookup
Origin: alias · Content-Type: application/json
{"q":"John Smith","ctx":{"org":"IBM","city":"New York","country":"US"},"nocache":1}
```

**State:** quiet (no L2 load) · sequential · sleep ~2.5s · same build.

**Result (שרת minrepro 2026-09-19):**
| i | top id | Q1701775 | wiki429 | rid |
|---|--------|----------|---------|-----|
| 1 | `ol-OL177707A` | no | 0 | `9fb0708f…` |
| 2 | `ol-OL177707A` | no | 7 | `c7dc5673…` |
| 3 | `ol-OL177707A` | no | 7 | `4cc16c8a…` |
| **4** | **`wd-Q1701775`** | **YES #1** | 8 | `8f2f4643…` |

Artifacts: `test-results/wp3/F-L2-ACC-001-MINREPRO-שרת-2026-09-19/`  
Also: Acc×3 quiet r1 PASS / r2 FAIL (`L2-ACC-X3-QUIET-דיוק-2026-09-19`).

**Trigger condition (isolated):** Stage-B **Wikidata search returns hits** for `John Smith` and city token `New York` appears in a WD description → score boost → `wd-Q1701775` sorts above OL/VIAF. When WD list empty/fails (429/timeout path without usable rows), top stays OL — **pw=0**.

---

## 2. Root-cause hypothesis + Evidence

### Hypothesis (primary)
**Stage-B open-registry Wikidata path emits Acc-banned QID `Q1701775` into `candidates[]` / `sources[]` without going through dossier commit gates.** City ctx `New York` substring-matches the WD description → `match: New York` → score ≈0.9 → ranks **#1**. Acc protocol: any `Q1701775` anywhere ⇒ **pw=1 hard**. `faces=0` / `qid=null` are expected on `ui=candidates` and **do not** scrub candidate IDs.

### Evidence
| Fact | Source |
|------|--------|
| Fail payload: `candidates[0].id=wd-Q1701775`, why includes `match: New York`, `qid=null`, `faces=false` | Acc×3 r2-smith · minrepro smith-4 |
| Clean payload: OL/VIAF only, no WD | Acc×3 r1 · minrepro 1–3 |
| WD search scores `0.55 + nameOverlap*0.25 + (ctxHit?0.12:0)` capped 0.9; `ctxHit` if city/org in `label+description` | `api/lib/stageB.js` `wikidataSearch` ~L243–285 |
| `whyFor` adds `match: ${city}` when description contains city | `stageB.js` `whyFor` ~L92–100 |
| Registry merge sorts by score; WD 0.9 > OL ~0.82 | `mergeCandidates` ~L290–310 |
| Early candidates return from evidenced Stage-B (≥2 hits) | `api/lookup.js` ~L3662–3692 |
| P0 comment: never `seedDossierFromKnown` on arbitrary Stage-B QID (Smith COLD PW) | `lookup.js` ~L2007–2009 · `orchestrator.js` header ~L25 |
| `mayCommitDossier` / demote clears faces+qid on candidates — **does not filter candidate ids** | `orchestrator.js` `mayCommitDossier` · demote ~L700–703 |

### Note (WD/Acc identity)
Live `wbsearchentities`/`wbgetentities` currently label **Q1701775 = John Smith, American politician from New York (1752–1816)**. Will Smith actor is **Q40096**. Acc lock still treats **Q1701775 as hard ban** (legacy Will-Smith PW / NEVER rule). RCA does **not** rewrite Acc Expected — presence of banned QID remains FAIL under current lock.

---

## 3. Involved state

| State | Role |
|-------|------|
| `q` / `ctx.org|city|country` | Input; city drives WD `ctxHit` |
| `nocache:1` | Bypasses response cache (`cached=null` on probes) |
| Stage-B registries | ORCID / OpenLibrary / VIAF / **Wikidata** parallel (`registryDiscover`) |
| WD 429/timeout counters | Correlate with stress; **not sufficient alone** — HIT also on quiet with wiki429≥6 when WD rows still return |
| `uiState=candidates` | Ambiguous path; `needCandidatePick=true` |
| `qid` / `faces` / images | Forced null/false on candidates emit |
| Acc lock rule | NEVER `Q1701775` in payload (candidates/sources/urls) |
| Multi-instance | Possible variance in which Stage-B deps succeed; no proof of sticky cross-instance cache for this fail (nocache) |

---

## 4. Why `faces=0` did not block binding/ranking

There is **no binding** on the fail path:
- Domain commit gate (`mayCommitDossier`) blocked dossier → `qid` stays null, faces scrubbed.
- Acc pw is defined on **QID string presence in candidates/sources**, not on face/dossier commit.
- Candidates UI is designed to show registry picks **without** committing identity — faces gate is orthogonal and already “won”; ranking still leaked the banned id.

---

## 5. cache / concurrency / dependency?

| Factor | Assessment |
|--------|------------|
| Response cache | Unlikely primary — `nocache:1`, `cached=null` on HIT |
| Concurrency / L2 load | Amplifies wiki429; **not required** — quiet minrepro HIT at i=4 |
| Dependency (Wikidata search) | **Primary intermittency** — WD rows present ⇒ leak; WD empty ⇒ OL top, Acc PASS |
| Race | Soft: Promise.allSettled registries; whichever WD/OL/VIAF return merge by score — non-deterministic under partial WD success |

---

## 6. Proposed fix (DO NOT IMPLEMENT)

1. **Acc-safe candidates scrub (preferred for Acc P0 gate):** strip / downrank / never emit `Q1701775` (and configurable poison set) from Stage-B `candidates` + `sources` before response — even when WD search returns it. Aligns with Acc NEVER rule without touching Expected.
2. **Narrow NY-boost:** do not apply city `ctxHit` score boost for ultra-common Latin names (John Smith class) on WD-only rows — reduces politician-NY false top-rank (measure carefully).
3. **Keep P0 commit guards** — already prevent dossier+faces from Stage-B QID; do not relax.
4. **Acc Gate process:** separate decision whether Q1701775 ban remains absolute if WD identity drifted to NY politician — product/Acc ownership; not a silent Expected rewrite.
5. After fix: regression Acc×3 quiet + load repro + then consider reopen WP3. **No WP4 until Acc Gate PASS.**

---

## Align
RECLASSIFY: intermittent ranking-leak on baseline quiet · SoT commit held · Acc P0 blocker.  
Pack refs: minrepro dir · Acc×3 quiet · this RCA · code `api/lib/stageB.js` · `api/lookup.js` Stage-B candidates · `api/lib/orchestrator.js` commit/demote.
