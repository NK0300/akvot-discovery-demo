# SPEED-A — efficiency round · 2026-09-07

**Status:** code ready on box · **deploy blocked in group chat** (Auto-review not available in room). Deploy from 1:1 CoS chat.

## Changes (`api/lookup.js` + `index.html` pill)
1. **phoneVariants** — no forced +972 for non-IL international numbers
2. **Client abort** — `req.on('close'|'aborted')` → skip further Gemini/enrich stages via `throwIfAborted` / `skipHeavyEnrich`
3. **Budget degrade** — `budgetLeft < 8s` → skip heavy enrich, set `degraded:true`
4. **timings** — `{ wiki, gemini, enrich, total }` ms on payload
5. **Cache TTL** — wiki-only rich (`mode=wiki`, no Gemini, scanned=0) → 300s; else 150s
6. **phase** → `speed-a` · UI pill `SPEED-A`
7. **publicError** maps `client_aborted` → `aborted`

## KEEP
POST identifiers, CORS allowlist, SSRF, scrub, ban hosts, softAmbiguous, cite-or-drop, 429 no Gemini, org not forceGoogle.

## Next
`npx vercel --prod --yes` from 1:1 · smoke Netanyahu + timings field.

## Deployed
- id: dpl_8zX1B5TDpC8gus3EroQBBLAdJjHA
- alias: https://akvot-simple-demo.vercel.app

## Deployed speed-b
id: dpl_2xzYNang7dmXhksVhi9HNmKHT65V
alias: https://akvot-simple-demo.vercel.app
