# HARDEN-A — Critical + High fixes — 2026-09-07 (Asia/Jerusalem)

Live: https://akvot-simple-demo.vercel.app  
Deploy: https://akvot-simple-demo-9z7w0rutv-k-akvot.vercel.app (aliased)  
Deployment id: `dpl_AV9sBYSq5tK8uUWmHspC5AaHhwWb`  
Phase stamp: `harden-a` (UI pill `HARDEN-A`)

Preserved teammate מ P0 (`dpl_6W3Ve5jeqFtQa5vkB83sspKkTZ72`, see `P0-FIX-2026-09-07.md`):
CORS allowlist (not `*`), 403 unknown Origin, 405 non-GET, rate-limit 20/10min, optional `AKVOT_DEMO_TOKEN`, `safeFetchPage` SSRF with manual redirects. **Not reverted.**

## Files changed
- `api/lookup.js` — scrub URLs, bans, Gemini header, wiki429, softAmbiguous, rich early skip, one-Gemini, phase; P0 blocks kept
- `index.html` — `safeUrl` allowlist, Cancel + no non-SSE auto-retry, HARDEN-A pill
- `vercel.json` — `regions: ["fra1"]`, Referrer-Policy no-referrer, X-Content-Type-Options nosniff, X-Frame-Options DENY, Permissions-Policy

## Round 1+2 checklist

| # | Item | Status |
|---|------|--------|
| 1 | Scrub phone/email in `sources[].url` / images / preview / extra_facts; drop `tel:` | **Done** (`scrubUrlField` + `scrubPayloadIdentifiers`) |
| 2 | Expand `BANNED_PHONE_HOSTS` TLDs (truecaller.in/app, getcontact.app, sync.me variants, eyecon, …) | **Done** |
| 3 | Gemini key via `x-goog-api-key` header (not query) | **Done** |
| 4 | CORS lock (not `*`) | **Done by מ** (allowlist + 403); kept |
| 5 | Wiki 429/503 → no Gemini unless phone/email/focus | **Done** (`forceGeminiOnWiki429`; org-only does not force) |
| 6 | softAmbiguous not cleared by weak google+context | **Done** (needs focus approve or wiki committed QID) |
| 7 | wikiIsRich early return: skip enrichFromPages / Bing / Commons-if-photo | **Done** (`skipHeavyEnrich`) |
| 8 | UI `safeUrl` https(/http) allowlist; reject javascript:/data: | **Done** |
| 9 | UI Cancel; abort fetch; **no** full non-SSE auto-retry | **Done** |
| 10 | SSRF gate scan/enrich/grounding | **Done by מ** (`safeFetchPage` + assertSafe…); kept |
| 11 | One Gemini max (no `googleLinksOnly` second call) | **Done** (`linksBoostPromise = []`; grounding from first call) |
| 12 | vercel.json security headers + fra1 + maxDuration 60 | **Done** |
| 13 | Phase `harden-a` | **Done** |

## Smoke (UTC times ≈ wall; user zone Asia/Jerusalem UTC+3)

| Case | HTTP | time_total | Result |
|------|------|------------|--------|
| OPTIONS + allowed Origin | 204 | — | `ACAO: https://akvot-simple-demo.vercel.app` (not `*`); security headers present |
| OPTIONS + evil Origin | 403 | — | `{"error":"origin not allowed"}` (מ) |
| Netanyahu `nocache=1` | 200 | **~3.86s** | `phase=harden-a`, `mode=wiki`, Q43723, **scanned=0**, photo+2 images, not ambiguous |
| Danny (common EN) | 200 | **~37.0s** | `mode=candidates`, ambiguous, **0 images / no photo**, needCandidatePick |
| Danny + city Tel Aviv | 200 | **~45.5s** | This run: wiki resolved unique-ish hit (`mode=wiki`, photo) — **not** softAmbiguous-clear-by-weak-SERP; wiki commit path |
| Homepage | 200 | — | Pill `HARDEN-A`; Referrer-Policy / nosniff / DENY / Permissions-Policy |

## Key behavior changes
- Rich wiki dossiers (e.g. Netanyahu) skip Gemini + page scrape → ~4s, `scanned=0`.
- Common / ambiguous names stay on candidates with **0 faces** unless focus approve or wiki QID commit.
- Phone/email identifiers scrubbed out of payload URLs (path/query/hash) and `tel:` filtered.
- At most one Gemini `generateContent` per request; grounding URLs reused instead of `googleLinksOnly`.
- Browser Cancel aborts in-flight fetch; stream failure does **not** kick a second non-SSE lookup (avoids double Gemini).

## Leftover risks
- In-memory rate limit is per-isolate (noted in P0 report).
- `AKVOT_DEMO_TOKEN` still unset → public demo open (optional to set in Vercel).
- Static HTML may still advertise CORS idiosyncrasies from edge; **API** CORS is locked.
- EN common names: wiki sometimes uniquely resolves (Danny+city this run) → dossier OK when wiki commits; flaky wiki 429s still skip Gemini when no phone/email/focus.
- `googleLinksOnly` function remains in file but is **not called**.
- Build machine showed cle1; function routing includes `fra1` in `x-vercel-id` (`cle1::fra1::…`).

## Privacy invariants kept
Public sources only; Truecaller/Sync.me/GetContact/Eyecon banned (expanded TLDs); soft-ambiguous → 0 faces; phone = search trigger only; cite-or-drop / dryBio wiki-first; no invented faces for ambiguous names.
