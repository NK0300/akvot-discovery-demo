# F-L2-ACC-001 · ARCH RCA · ארכיטקט · 2026-09-19
**Mode:** ROOT CAUSE ONLY · **NO fix · NO patch · NO dpl**  
**Align:** accepts `F-L2-ACC-001-RCA-שרת-2026-09-19.md` · RECLASSIFY quiet intermittent

---

## 1. Minimal reproduction (Arch accept)
Quiet `POST` Smith+IBM+NY+US · `nocache:1` · same `dpl_7vAA…` · sequential → intermittent **candidates[0]=wd-Q1701775** · primary `qid=null` · `faces=0`.

---

## 2. Root-cause (architecture)
**Contract gap, not commit failure:**

| Layer | Contract | Result on fail path |
|-------|----------|---------------------|
| **SoT / Domain** | Never auto-dossier Smith-class without trusted seed | **PASS** — `ui=candidates` · no primary qid/faces |
| **Acc lock** | NEVER `Q1701775` **anywhere** in payload | **FAIL** — id in `candidates[]` / sources |
| **Stage-B** | WD search + city `ctxHit` → score 0.9 → sort #1 | **EMIT** poison QID into pick list |

P0 trusted-seed fix closed **commit** path only. It never claimed to scrub Stage-B candidate IDs. Ranking/selection Acc risk remained latent; quiet minrepro proves it.

---

## 3. Involved state
q+ctx · Stage-B WD/OL/VIAF merge · score sort · early candidates emit · Acc NEVER rule · WD upstream intermittency. Response cache not primary (`nocache`).

---

## 4. Why faces=0 did not block Acc
`faces` / primary `qid` gate **dossier commit**, not Acc string-ban.  
Candidates mode **intentionally** lists registry IDs without committing — Acc still scans those IDs. Orthogonal gates: SoT won; Acc lost.

---

## 5. cache / concurrency / dependency
- Cache: not required for fail  
- Load: amplifies, not sole cause  
- **Wikidata dependency:** primary intermittency driver  

---

## 6. Proposed fix (DO NOT IMPLEMENT)
1. **Domain/App candidates scrub** (Acc P0 gate): before emit, drop/downrank Acc-banned QIDs on Smith-class (class-level denylist / mismatch scrub) — **separate Gate**, not silent Expected rewrite.  
2. Optional: soften NY `ctxHit` for common Latin bare+ctx (measure Acc side-effects).  
3. Keep all P0 commit/trusted-seed guards.  
4. Process: Acc Gate PASS → then reopen WP3; **WP4 blocked** until then.

---

## Arch verdict
| Item | Call |
|------|------|
| SoT | **HELD** |
| Acc | **P0 blocker** on frozen prod |
| WP3 | **FAIL Acc gate** |
| WP4 | **NO GO** |
| Next | Preserve Evidence · Chief Acc P0 Gate decision · no patch now |

**FAIL → PRESERVE → ROOT CAUSE (done) → (later) FIX → REGRESSION → RE-GATE**
