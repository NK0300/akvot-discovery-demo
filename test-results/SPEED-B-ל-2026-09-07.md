# SPEED-B — latency + reliability · ל · 2026-09-07

## Changes
1. **Open Library** — with Wikidata P648 (`olid`): return immediately, no fuzzy `/search/authors.json`. Rich wiki path uses olid-only (`fuzzy:false`).
2. **Pure candidates** — softAmbiguous without Gemini/wiki: skip OL + grounding resolve entirely.
3. **resolveSourceUrls** — only Google grounding hosts, cap 8, concurrency 6; non-grounding passthrough.
4. **Cache** — wiki-only TTL 300→480s; default 150→180s (still no PII cache).
5. **Wiki 429 retry** — backoff sleep 600→350ms.
6. **phase** → `speed-b` · UI pill `SPEED-B`.

## KEEP
SSRF, scrub, CORS, POST identifiers, cite-or-drop, AbortSignal hooks from מ, softAmbiguous gates.

## Check
`node --check` passed. Ready for merge with מ AbortSignal patch.
