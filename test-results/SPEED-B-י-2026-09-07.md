# SPEED-B — latency + reliability · י · 2026-09-07

## Changes (`api/lookup.js` + `index.html`)
1. **Commons** — parallel term fetches; 4.5s / 1 retry; no bare-name search when QID present.
2. **Skip gallery work** — Commons skipped whenever portrait already set; Bing skipped if portrait OR ≥2 Gemini images (or rich/skipHeavy).
3. **enrichFromPages** — scan cap 8→5, concurrency 5→4.
4. **bingImages** — 7.5s→4.5s + `fetchSignal` (client abort mid-fetch).
5. **Open Library** — google-only with ≥4 sources skips fuzzy OL.
6. **UI** — chip for `timings.total` (שניות) + `מקוצר` when degraded.

## KEEP
AbortSignal from מ, ל speed-b (OL P648 / pure candidates / resolve cap / cache TTL), security hardenings.

## Check
`node --check` — see shell.
