# 05 — UX SSE DEEP · ממשק · 2026-09-20

**Overall: PARTIAL** · **Promote: HOLD** · **Deploy: none** · **Core `dpl_8ag…`: UNTOUCHED**

| Field | Value |
|-------|-------|
| Role | ממשק (UX / SSE deep) · PR-CLOSEOUT B17/B18 |
| Stamp | 2026-09-20 ~08:48–08:50 IDT (Asia/Jerusalem) |
| Canonical Preview | `dpl_9PkJ5WJFEEYMjpSr5W8mf1GDv6A2` |
| URL | `https://akvot-simple-demo-2v59j4kvl-k-akvot.vercel.app` |
| Method | Code audit of local `discovery-ui.js` + live `vercel curl --deployment dpl_9PkJ… --scope k-akvot` |
| storeBackend (live) | **upstash** · durable=true · promoteEligible=true · fallback=false |
| Client bytes | 55,458 — **local ≡ Preview** (diff empty) |
| Raw | `PR-CLOSEOUT/raw/sse-deep-9PkJ-2026-09-20/` |
| Prior | `MEGA-M-CANONICAL-9PkJ-VERIFY-ממשק-2026-09-20.md` · `MEGA-M-SSE-LIVE-UX-ממשק-2026-09-20.md` |

---

## 1. Checklist

| # | Item | Status | Evidence |
|---|------|--------|----------|
| 1 | Reconnect with cursor / Last-Event-ID | **GREEN** | **Client:** `runViaSse` → `buildUrl()` appends `?cursor=` + `?lastEventId=` from `discState.cursor`; `noteSseCursor` tracks `ev.lastEventId` / payload `cursor`. Close-on-error disables browser auto-reconnect thrash; manual backoff cap 5 (`SSE_MAX_RECONNECT`). **Live:** `?lastEventId=5&cursor=5` → frames **id 6–9 only** (skip 1–5). `?lastEventId=99` → hang-guard `done` id 100 `resumed:true` / `replay complete — no new events`. Headers: `X-SSE-Reconnect: Last-Event-ID`. Spec limit: EventSource cannot set custom `Last-Event-ID` header on construct — mitigated via query (server accepts header **or** query). |
| 2 | Abort / close EventSource cleanup | **GREEN** | **Client audit:** `closeSse()` · `clearDiscTimers()` closes ES + clears timers · `finish()` removes abort listener, closes ES, settles · `signal.addEventListener('abort', onAbort)` · `startDiscovery` / cancel abort prior `AbortController` then `clearDiscTimers`. No orphan reconnect after abort. **Live:** N/A browser abort (CLI finite stream ends cleanly with `done` + `res.end`). |
| 3 | Duplicate finding merge-by-id | **GREEN** | `mergeById` / `mergeFindingChunk` / `metaMergeHint` on snapshot + SSE finding/replay. Empty non-narrow snapshot cannot wipe good feed. Replay×2 same session yields identical finding id `wp-en-Example_org` once in each stream; client merge would keep one card. |
| 4 | Event order / terminal stop (no thrash) | **GREEN** | Live order: `meta → progress → provider×3 → finding → facets → status → done` (ids 1…9). Client: `done`/`complete`/`terminal` → `settleTerminalStatus` + `reachedTerminal` + `closeSse`; `onerror` short-circuits if underlying terminal; max reconnect → GET fallback `sse→get`. No infinite reconnect loop in code. |
| 5 | Replay-from-complete | **GREEN** | **Client:** `hydrateSession` GET-first paint → optional SSE with cursor cleared for full replay + merge. **Live:** events×2 both HTTP 200, 9 frames, non-empty, terminal `done`. |
| 6 | Narrow then HIT/GET consistency | **YELLOW** | **Narrow HTTP 200** OK. `kind=page` → before=1 after=1 v3. `kind=registry` → before=1 **after=0** v4 (response findings=[]). **GET after registry narrow still returns 1 finding** (`wp-en-Example_org`); no `narrow`/`lastNarrow` on HIT. **SSE after narrow** still emits full finding. Root cause (server): `narrowDiscoverySession` persists `raw.lastNarrow` + `narrowActive` but **does not replace** `raw.findings`; `getDiscoverySession` / `buildProgressiveEvents` emit full `findings`. **Same-session UI OK:** client `postNarrow` → `applySnapshot(..., narrowSource:'server')` paints filtered set (incl. empty). Reload/HIT/SSE projection = **OPEN (server)**. |
| 7 | Partial → final UI states | **GREEN** | States: `running` / `partial` / `complete` / `failed_soft` / `reconnecting`. `isTerminalStatus(partial)` when no provider `pending`. Live session settled `partial` (wikidata=error) with `done`. `preserveTerminal` blocks terminal→running regression on reconnect paint. Progress strip classes + HE labels. |
| 8 | No identity chrome | **GREEN** | Discovery banner: `INFORMATION ≠ IDENTITY` · `אין דיוקן / תיק זהות / «זה האדם»`. Footer note same. `stripIdentityChrome` on apply paths. Live GET/SSE/narrow JSON: no `dossier`/`faces`/`photoUrl`/`identityCommit`/`Q1701775`. (Core Entity dossier UI remains in `index.html` under non-discovery mode — out of Discovery surface.) |

Machine: `05-UX-SSE-DEEP-STATUS.json`.

---

## 2. Live smoke (dpl_9PkJ)

Sequence: **POST → GET → events×2 → cursor-resume → narrow → GET again**

| Step | Result |
|------|--------|
| `GET /api/health` | 200 · build=`dpl_9PkJ…` · `discoveryStore.storeBackend=upstash` · durable · promoteEligible |
| `GET /api/discovery/health` | 200 · WRITE/READ/UPDATE/DELETE · mode=`kv-shared` · latencyMs≈115 |
| `GET /discovery-ui.js` | 200 · 55458 B · MEGA-M symbols present |
| POST `{"q":"example.org"}` | **201** · `sessionId=kv1.5496b081a5875d1c0b20338d3d6689da` · status=`partial` (wikidata error) · 1 finding · **storeBackend=upstash** |
| GET same | **200** · durable hit · same id · upstash |
| events #1 | **200** · `text/event-stream` · 9 frames · terminal `done` |
| events #2 (replay) | **200** · same shape · non-empty |
| events `?lastEventId=5` | **200** · ids 6–9 only |
| events `?lastEventId=99` | **200** · hang-guard done |
| POST narrow `kind:["registry"]` | **200** · 1→0 · version 4 |
| GET after narrow | **200** · **still 1 finding** (see YELLOW / OPEN) |

`storeBackend` noted on health, POST store, GET store, SSE meta: **upstash** throughout.

---

## 3. Client changes this wave

**None.** Local `discovery-ui.js` already matches Preview (identical 55458 bytes). Prior MEGA-M harden covers reconnect/cursor/abort/merge/hydrate/identity. No thin+safe gap that is client-only without server `lastNarrow` projection.

---

## 4. OPEN ITEMS

| ID | Owner | Severity | Item |
|----|-------|----------|------|
| O1 | שרת / orchestrator | **P1 for closeout bar** | **HIT/GET + SSE must project `lastNarrow` when `narrowActive`** (or replace stored findings). Today narrow response is correct; durable GET/SSE ignore `lastNarrow`. Blocks checklist item 6 GREEN. |
| O2 | ממשק (known) | Soft | Browser EventSource cannot set `Last-Event-ID` header on first construct — query `cursor`/`lastEventId` mitigation is live-proven. |
| O3 | Soft | Soft | This live POST landed `partial` (wikidata provider error); prior canonical had `complete` + 3 findings. Client terminal handling still GREEN; richer narrow 3→2 not re-proven this wave (registry 1→0 exercised instead). |

---

## 5. Locks held

| Lock | Status |
|------|--------|
| STOP before promote | **Held** |
| Core alias `dpl_8ag…` | **Untouched** |
| No deploy / no Preview redeploy | **Held** |
| No Core / lookup touch | **Held** |
| No secrets in Evidence | **Held** |

---

## 6. Files

| Path | Role |
|------|------|
| `discovery-ui.js` | Audited (unchanged this wave) |
| `PR-CLOSEOUT/05-UX-SSE-DEEP-ממשק-2026-09-20.md` | This report |
| `PR-CLOSEOUT/05-UX-SSE-DEEP-STATUS.json` | Machine checklist |
| `PR-CLOSEOUT/raw/sse-deep-9PkJ-2026-09-20/` | Live raw + `00-SMOKE-SUMMARY.json` |

**Decision:** SSE/reconnect/abort/dup/order/replay/partial→final/identity **GREEN**. Narrow→GET/SSE projection **YELLOW → OPEN O1 (server)**. **PARTIAL · HOLD promote.**
