# 20 — LATENCY

URL/domain seeds (web_origin active): ~2.5–3.5s end-to-end create (shared with other providers).  
Person seeds: dominated by WD/WP/OL/VIAF — web_origin idle (~0 extra fetch).  
No catastrophic regression observed vs B0 person-seed baselines (~3–6s).
