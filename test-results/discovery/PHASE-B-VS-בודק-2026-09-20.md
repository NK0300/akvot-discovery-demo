# Phase B · Discovery VS · בודק · 2026-09-20

**STATUS:** **PASS** · MEASURE ONLY · **NO PROMOTE**  
**Preview:** `https://akvot-simple-demo-p5zxut9y5-k-akvot.vercel.app`  
**dpl:** `dpl_BvWg9yUsibVqdrCAexSTMpwoabZ6` (health.build match)  
**Origin:** `https://akvot-simple-demo.vercel.app`  
**Access:** `vercel curl --deployment dpl_BvWg9yUsibVqdrCAexSTMpwoabZ6 --scope k-akvot`  
**Zone:** Asia/Jerusalem (UTC+3) · run ~02:53–02:54 IDT

---

## Gate

| Track | Result |
|-------|--------|
| Discovery health.build | **PASS** (`dpl_BvWg9yUsibVqdrCAexSTMpwoabZ6`) |
| VS-S1 `דוד כהן` | **PASS** |
| VS-S2 `Alex Morgan` | **PASS** |
| VS-S3 `example.org` | **PASS** |
| Multi-Seed / Entity-Agnostic | **PASS** |
| Acc-DISC leakage | **PASS** · **0** |
| Promote | **NOT DONE** |

**Overall VS:** **PASS**

---

## Endpoints (as implemented)

| Method | Path | Notes |
|--------|------|-------|
| `POST` | `/api/discovery/sessions` | body `{ seed }` (or `q`); returns `sessionId` + Acc-scrubbed **`snapshot`** |
| `GET` | `/api/discovery/sessions/:id` | poll snapshot; **cross-instance 404** expected (in-memory Map) |

SSE `/events` and `/narrow` **not** in this slice (poll/snapshot-only) — documented soft.

Validation used **POST.snapshot** for all 3 Seeds (GET returned `session not found` on different instance — expected per SERVER slice §5/§7).

---

## Per-Seed VS-S

| Seed | status | findings | evidence w/ provenanceUrl | facets | dossier/faces | forbiddenIdentitiesVersion | Acc leak |
|------|--------|----------|---------------------------|--------|---------------|----------------------------|----------|
| S1 `דוד כהן` | partial | 13 | 13/13 | OK (array) | none | `2026-09-19.1` | 0 |
| S2 `Alex Morgan` | partial | 15 | 15/15 | OK (array) | none | `2026-09-19.1` | 0 |
| S3 `example.org` | complete | 2 | 2/2 | OK (array) | none | `2026-09-19.1` | 0 |

### Sample provenance

- S1: `https://www.wikidata.org/wiki/Q88524142` (wikidata) · providers wikidata=partial, openlibrary=ok
- S2: `https://www.wikidata.org/wiki/Q233510` (wikidata) · providers wikidata=ok, openlibrary=partial
- S3: `https://www.wikidata.org/wiki/Q306656` (wikidata) · providers wikidata=ok, openlibrary=ok

### Schema / bans (all Seeds)

- ≥1 Finding with `id`/`kind`/`title`/`evidenceIds`/`providers`
- Evidence requires `provenanceUrl` (https) — all linked evidence IDs present
- **No** `dossier` / `faces` / identity-commit `photoUrl` on Discovery surfaces
- Facets: non-empty buckets with `key`/`label`/`buckets` (OK)

---

## Entity-Agnostic

Identical snapshot top-level keys across S1–S3:

`createdAt, evidence, facets, findings, forbiddenIdentitiesVersion, graph, progress, providers, q, seed, sessionId, softEr, stage, status`

Same `POST /api/discovery/sessions` path · no Seed-specific response shape · **PASS**.

---

## Acc-DISC

- Deep-scan all Discovery POST/GET raw JSON for `Q1701775` / `wd-Q1701775` (denylist class)
- **leakage = 0** across S1–S3 + health
- Inject harness: **not present** on live Preview — observe-only (per plan)
- `forbiddenIdentitiesVersion` present on all snapshots: `2026-09-19.1`

---

## Artifacts

| Path | Role |
|------|------|
| `test-results/discovery/PHASE-B-VS-בודק-2026-09-20.json` | Machine gate + per-seed checks |
| `test-results/discovery/PHASE-B-VS-raw/` | Raw POST/GET/health JSON + harness |
| `test-results/discovery/PHASE-B-VS-raw/run-phase-b-vs.mjs` | Repro harness |

---

**STOP for Chief Review · NO promote · Core alias untouched (see CORE-REGRESSION).**
